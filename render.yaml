services:
  - type: web
    name: nosy-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ai_cleaner backend.app:app 
    workingDirectory: ./backend
